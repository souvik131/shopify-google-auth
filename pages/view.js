import { Heading, Page } from "@shopify/polaris";

const Main = props => {
  return (<Page>
            <Heading>Congrats, you are logged in! 🎉</Heading> 
          </Page>);
}

export default Main;
